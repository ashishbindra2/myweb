angular 16 version

`npm install -g @angular/cli@16`

for angular packages from pacakage.json file 

`npm i`

make sure pacakage.json file available 


after that to open the homepage 

if we want to open directly in the browser we write

`ng serve --open`

or we can simple write 

`npm run start` both commands works as same
